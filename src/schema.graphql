type Mission {
  title: String!
}

type Mutation {
  # name the mutation, Type of the passed in vars and finally :[returns this] 
  createMission(title: String!): Mission
}

type Query {
  missions: [Mission]!
}
